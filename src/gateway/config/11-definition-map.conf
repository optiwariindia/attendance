map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

map $scheme $hsts_header {
    https "max-age=63072000; preload";
}
map $host $env {
    default                         prod;

    dev.sampledge.duckdns.org       dev;
    *.sampledge.com                 prod;
}